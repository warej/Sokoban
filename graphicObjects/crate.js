var crateCounter = 0;

function crate () {
	
}

crate.prototype.setPosition = function (x, y, z) {
	this.x = x;
	this.y = y;
	this.z = z;
};

crate.prototype.setRotation = function (rotation) {
	this.rotation = rotation;
};

crate.prototype.move = function (dx, dy, dz) {
	
};

crate.prototype.draw = function () {
	
};